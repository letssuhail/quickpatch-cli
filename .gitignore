.dart_tool/
.packages
pubspec.lock
build/
.idea/
.vscode/

# Built CLI snapshot + bundled Flutter/engine when this checkout is used as the
# QuickPatch install root (~/.quickpatch). Everything under bin/cache is
# generated by the wrapper / first run.
bin/cache/

# Local FVM pin (developer-machine only).
.fvm/
.fvmrc
