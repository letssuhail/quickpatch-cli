name: dex
description: DEX binary parser and semantic differ for Android Dalvik Executable files.
version: 0.1.0

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  meta: ^1.16.0

dev_dependencies:
  path: ^1.9.1
  test: ^1.25.0
