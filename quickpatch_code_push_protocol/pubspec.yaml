name: quickpatch_code_push_protocol
description: Library which contains common interfaces used by QuickPatch
version: 0.1.0+1
repository: https://github.com/letssuhail/quickpatch


environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  collection: ^1.19.1
  meta: ^1.16.0

dev_dependencies:
  mocktail: ^1.0.5
  # Only used by `tool/gen.dart` to regenerate lib/src/ from the OpenAPI
  # spec.
  space_gen: ^1.2.2
  test: ^1.25.0
